# Use Agent Platform/ADK built-in metrics only. Custom metrics are intentionally
# omitted: per-case rubric groups caused Vertex critique-parser failures when
# assertion counts and judge properties diverged.
metrics_to_run:
  - final_response_quality
  - instruction_following
  - tool_use_quality
  - hallucination
